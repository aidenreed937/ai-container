{
  "name": "AI Full-Stack Env",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:1-20",
  "postCreateCommand": "sh .devcontainer/post-create.sh",
  "containerEnv": {
    "CODEX_API_KEY": "${localEnv:AI_CONTAINER_CODEX_API_KEY}",
    "GEMINI_API_KEY": "${localEnv:AI_CONTAINER_GEMINI_API_KEY}",
    "GOOGLE_GEMINI_BASE_URL": "${localEnv:AI_CONTAINER_GOOGLE_GEMINI_BASE_URL}",
    "ANTHROPIC_API_KEY": "${localEnv:AI_CONTAINER_ANTHROPIC_API_KEY}",
    "ANTHROPIC_BASE_URL": "${localEnv:AI_CONTAINER_ANTHROPIC_BASE_URL}",
    "AI_CONTAINER_UNATTENDED": "${localEnv:AI_CONTAINER_UNATTENDED}",
    "AI_CONTAINER_BOOTSTRAP_PROFILE": "${localEnv:AI_CONTAINER_BOOTSTRAP_PROFILE}",
    "AI_CONTAINER_BOOTSTRAP_PROMPT_FILE": "${localEnv:AI_CONTAINER_BOOTSTRAP_PROMPT_FILE}",
    "AI_CONTAINER_BOOTSTRAP_PROMPT": "${localEnv:AI_CONTAINER_BOOTSTRAP_PROMPT}",
    "AI_CONTAINER_LOG_DIR": "${localEnv:AI_CONTAINER_LOG_DIR}"
  },
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "zsh"
      },
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "pkief.material-icon-theme"
      ]
    }
  },
  "forwardPorts": [3000],
  "remoteUser": "node",
  "mounts": [
    "source=${localWorkspaceFolder}/.codex,target=/home/node/.codex,type=bind",
    "source=${localWorkspaceFolder}/.gemini,target=/home/node/.gemini,type=bind",
    "source=${localWorkspaceFolder}/.claude,target=/home/node/.claude,type=bind"
  ]
}
