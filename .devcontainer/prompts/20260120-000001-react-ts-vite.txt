创建了reat+ts+vite的项目例子。

请在仓库内创建示例项目目录：demo/react-ts-vite

要求（必须遵守）：
- 只允许修改/新增：demo/react-ts-vite（不要改仓库其它任何文件）
- 如果 demo/react-ts-vite/package.json 已存在，则直接退出，不要重复创建
- 在 demo/ 目录执行：npm create vite@latest react-ts-vite -- --template react-ts --no-interactive
- 然后在 demo/react-ts-vite 下执行：npm install 与 npm run build
- 如果命令失败，请输出失败原因并退出

